class NoInternet {
  var msg;
  NoInternet(this.msg);
}

class NoServiceFound {
  var msg;
  NoServiceFound(this.msg);
}

class InvalidFormat {
  var msg;
  InvalidFormat(this.msg);
}

class Unknown {
  var msg;
  Unknown(this.msg);
}
